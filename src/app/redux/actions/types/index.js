export const GRID = {
    ADD: 'app/grid/ADD',
    RESET: 'app/grid/RESET',
}

export const PLAYERS = {
    SET: 'app/player/SET',
}

export const WINNER = {
    SET: 'app/winner/SET',
}

export const CURRENT_PLAYER = {
    SET: 'app/current_player/SET',
}

export default {
    GRID,
    GAME: {
        PLAYERS,
        CURRENT_PLAYER,
        WINNER,
    },
}
